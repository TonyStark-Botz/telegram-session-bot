# Core Telegram Bot Dependencies
pyrogram>=2.0.106,<2.1.0  # Pinned to stable 2.0.x series
pyromod>=2.0.0,<2.1.0      # For Pyrogram extensions
tgcrypto>=1.2.5,<2.0.0     # Encryption library

# Database
pymongo>=4.3.3,<4.4.0      # MongoDB driver

# Configuration
python-dotenv>=0.21.0,<1.0.0  # Environment variables

# Web Server (for health checks)
fastapi>=0.95.2,<0.96.0
uvicorn>=0.22.0,<0.23.0     # ASGI server
httptools>=0.5.0,<0.6.0     # Required by uvicorn
uvloop>=0.17.0,<0.18.0      # Optional but recommended for performance

# Runtime Utilities
python-multipart>=0.0.6,<0.0.7  # Required by FastAPI
